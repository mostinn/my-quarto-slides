## Start a git session in the directory (and get the right directory for the git tab):

#git init
git add Synthetic-biology.html
git commit -m "Add HTML of SynBio"
git remote add origin https://github.com/mostinn/brandeis.git
git push -u origin master

#_ Just push your changes:
git add .
git commit -m "Add slides"
git push

